package com.company;

public class SalirDelPrograma {

/*
3.4. Salir del programa
Si queremos salir programáticamente del programa actualmente ejecutado, System.exit hará el truco.
Para invocar la salida , necesitamos especificar un código de salida, que se enviará a la consola o
shell que inició el programa.
Por convención en Unix, un estado de 0 significa una salida normal, mientras que un valor distinto de cero
significa que ocurrió algún error:

	if (error) {
    System.exit(1);
} else {
    System.exit(0);
}

Tenga en cuenta que para la mayoría de los programas de hoy en día, sería extraño tener que llamar a esto. Cuando
se llama en una aplicación de servidor web, por ejemplo, ¡puede destruir todo el sitio!
*/


}
